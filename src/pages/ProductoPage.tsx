import { Typography } from '@mui/material';

export const ProductoPage = () => {
  return (
    <div>
      <Typography variant="h4">Página de Productos</Typography>
      <Typography>
        El contenido para la gestión de productos irá aquí.
      </Typography>
    </div>
  );
};